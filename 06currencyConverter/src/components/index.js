import InputBox from "./InputBox";

//we can directly import inputbox in app.jsx but for large project we have many components so we import all in one file and we use that file in app.jsx 

export {InputBox}